# v0.4 Smart Input Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** implement smart Enter/Tab/Backspace list behaviors, command refactor, configurable keymap, and API updates for v0.4.

**Architecture:** keep the existing controller/command architecture, split commands into dedicated modules under `src/commands/`, and surface keymap via a dedicated compartment.

**Tech Stack:** React, TypeScript, CodeMirror 6, Vitest.

---

## Recheck Summary

Current v0.4 work is partially implemented in the dirty worktree. The remaining completion pass is:

- Keep the existing command-directory refactor and verify it against the v0.4 spec.
- Fix smart-input tests before changing behavior when the test expectation contradicts the spec.
- Fix smart Tab selection mapping for outdent at document start.
- Run `npm test`, `npm run lint`, and `npm run build` before calling the milestone complete.
- Leave unrelated `.codex/` deletions untouched.

### Task 1: Command module refactor

**Files:**
- Create: `src/commands/inline.ts`, `src/commands/block.ts`, `src/commands/insert.ts`, `src/commands/editing.ts`, `src/commands/smartEnter.ts`, `src/commands/smartTab.ts`, `src/commands/smartBackspace.ts`, `src/commands/command-registry.ts`
- Modify: `src/commands/index.ts`
- Modify: `src/commands/list-syntax.ts`

- [x] Replace old monolithic command logic with moduleized exports using `state.changeByRange`.
- [x] Add `toggleHeading(level)` command and remove `toggleHeading1`..`6` exports.
- [x] Add ordered-list renumber behavior in `toggleOrderedList`.
- [x] Implement insert-command cursor placement updates (`insertLink`, `insertCodeBlock`, `insertTable`).
- [x] Add smart editing command builders in `smartEnter.ts`, `smartTab.ts`, `smartBackspace.ts`.

### Task 2: Controller and prop updates

**Files:**
- Modify: `src/types.ts`, `src/controller.ts`, `src/components/NeutrinoEditor.tsx`

- [x] Add `tabIndents` prop (`default: true`) and configurable `keymap` prop (`KeyBinding[] | ((defaults) => KeyBinding[])`).
- [x] Add keymap compartment and default keymap builder in `EditorController`.
- [x] Wire handlers so Enter/Shift-Enter/Mod-Enter/Backspace/Tab/Shift-Tab use smart command paths with defaults.
- [x] Support `Mod-K` for insert link and keep callback-based `onEnter/onEscape/onSubmit` semantics.

### Task 3: Tests

**Files:**
- Modify: `src/commands.test.ts`
- Add: `src/commands/smartEnter.test.ts`, `src/commands/smartTab.test.ts`, `src/commands/smartBackspace.test.ts`
- Modify: `src/controller.test.ts`, `src/components/NeutrinoEditor.test.tsx`, `src/components/NeutrinoEditor.stories.tsx`, `src/App.tsx`

- [x] Update heading tests to use `toggleHeading(level)`.
- [x] Add regression tests for smart enter/list continuation behavior, tab behavior, and smart backspace.
- [x] Add tests for keymap/`tabIndents=false` defaults and `keymap` override behavior.

### Task 4: Finish-up

**Files:**
- Modify: `CHANGELOG.md`

- [x] Add `## 0.4.0` notes for breaking `toggleHeadingN` removal, smart input behaviors, and keymap overrides.
- [x] Run lint and test commands.
