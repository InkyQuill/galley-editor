name: Publish Current Version

on:
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    name: Publish package.json version
    runs-on: ubuntu-latest
    if: ${{ vars.NPM_PUBLISH_ENABLED == 'true' }}
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
          registry-url: https://registry.npmjs.org

      - name: Install
        run: npm ci --legacy-peer-deps --no-audit --no-fund

      - name: Verify
        run: |
          npm run lint
          npx tsc -b packages/galley-editor
          npm run build:themes
          npm run test:coverage
          npm test --workspace @inkyquill/galley-themes
          npm run build:lib
          npm run test:package-consumer
          ./scripts/verify-publish.sh

      - name: Publish
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          npm publish --workspace @inkyquill/galley-editor --access public --provenance
          npm publish --workspace @inkyquill/galley-themes --access public --provenance
